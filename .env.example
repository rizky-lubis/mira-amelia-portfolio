# VITE_ADMIN_PASSWORD: Kata sandi portal admin (akses via URL /#admin atau ?admin=true).
# Diisi oleh pemilik situs saja. Tanpa nilai ini, dipakai fallback bawaan.
# JANGAN commit password asli ke repo — file .env / .env.local di-ignore oleh git.
VITE_ADMIN_PASSWORD="GANTI_DENGAN_PASSWORD_KAMU"
